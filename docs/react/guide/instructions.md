# 特殊说明

## 百万级数据

目前受限于浏览器最高高度限制，导致滚动条超出一定高度后无法继续滚动。在示例中，大约上线为38万行数据。后续作者计划通过自己模拟滚动条来实现百万级数据的展示

## 滚动性能

本虚拟列表支持满帧渲染，但在大数据或复杂dom场景下应当由使用者自行避免以下问题：

1. 列表数据建议使用 `useMemo` 或 `useRef` 避免不必要的重渲染。
2. 复杂dom应当使渲染层和交互层分离，渲染层dom要极简。
